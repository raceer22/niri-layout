[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "niri-layout"
version = "0.1.0"
description = "Multi-monitor workspace layout manager for Niri"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
niri-layout = "niri_layout.cli:main"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]